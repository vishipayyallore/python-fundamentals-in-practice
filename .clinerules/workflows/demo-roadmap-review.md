# Workflow: demo-roadmap-review (deprecated)

This workflow name is retired. Use **`session-roadmap-review`** from `.clinerules/workflows/session-roadmap-review.md` instead.

Tags: `session-review`, `curriculum`, `meetup`, `deprecated`
