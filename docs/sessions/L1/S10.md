---
learning_level: "Noob → Nerd"
prerequisites: 
  - "docs/sessions/L1/S9.md"
estimated_time: "45-60 minutes"
session_type: "Mini Project"
learning_objectives:
  - "Apply dictionaries for structured data storage"
  - "Use lists to manage collections"
  - "Combine loops with data structures"
  - "Create an interactive multi-profile application"
  - "Practice input validation and user experience"
related_topics:
  prerequisites:
    - "docs/sessions/L1/S8.md"
    - "docs/sessions/L1/S9.md"
  builds_upon:
    - "docs/sessions/L1/S1.md"
    - "docs/sessions/L1/S2.md"
    - "docs/sessions/L1/S3.md"
    - "docs/sessions/L1/S4.md"
    - "docs/sessions/L1/S6.md"
    - "docs/sessions/L1/S7.md"
    - "docs/sessions/L1/S8.md"
    - "docs/sessions/L1/S9.md"
  enables:
    - "docs/sessions/L2/S1.md"
  cross_refs:
    - "docs/sessions/L1/S5.md"
---

# Mini Project 2: Personal Profile Generator

**Duration:** 45-60 minutes  
**Type:** 🎯 Mini Project (Phase B Capstone)  
**Level:** Noob → Nerd

---

## 🎯 **Project Overview**

### **What You'll Build:**

A **Personal Profile Generator** that:

- ✅ Collects user information (name, age, city, hobbies, goals)
- ✅ Stores profiles using dictionaries
- ✅ Manages multiple profiles in a list
- ✅ Displays formatted profile cards
- ✅ Allows searching and editing profiles
- ✅ Calculates statistics about profiles

### **Skills Applied:**

| Session | Concepts Used                                    |
| ------- | ------------------------------------------------ |
| S1-S2   | Variables, strings, input/output                 |
| S3      | Operators, string formatting                     |
| S4      | Conditionals, Boolean logic                      |
| S6      | Loops (`for`, `while`), `break`/`continue`       |
| S7      | Error handling, built-in functions               |
| S8      | Lists, list methods, iteration                   |
| S9      | Dictionaries, dictionary methods                 |

---

## 📋 **Project Requirements**

### **Core Features:**

1. **Create Profile** - Collect and store user information
2. **View All Profiles** - Display list of all profiles
3. **View Single Profile** - Show detailed profile card
4. **Search Profiles** - Find profiles by name
5. **Edit Profile** - Modify existing profile data
6. **Delete Profile** - Remove a profile
7. **Profile Statistics** - Show summary statistics

### **Profile Structure:**

Each profile should contain:

- **Name** (string) - Required
- **Age** (integer) - Required, validated 1-150
- **City** (string) - Optional (defaults to `"Not specified"` if left blank)
- **Hobbies** (list) - At least one hobby
- **Goals** (list) - At least one goal

---

## 🏗️ **Building the Project**

### **Step 1: Project Setup**

Create the file structure:

```text
📁 src/
└── 📁 L1/
    └── 📁 S10/
        └── 📄 profile_generator.py
```

### **Step 2: Start with the Template**

> 🧭 **Guided scaffold note:** The `TODO` markers in the template below are intentional learning checkpoints for students during the build process.  
> ✅ For a complete reference implementation, see `src/L1/S10/profile_generator.py`.

```python
# Filename: src/L1/S10/profile_generator.py

"""
👤 Personal Profile Generator
Level 1 Capstone Project - Phase B

A complete application demonstrating:
- Dictionaries for structured data
- Lists for collections
- Input validation
- Menu-driven interface
"""

# Store all profiles in a list
profiles = []


def create_profile():
    """Collect user information and create a new profile."""
    print("\n" + "=" * 50)
    print("📝 CREATE NEW PROFILE")
    print("=" * 50)
    
    # Collect name
    name = input("Enter name: ").strip()
    if not name:
        print("❌ Name cannot be empty!")
        return None
    
    # TODO: Check if name already exists
    
    # Collect age with validation
    while True:
        try:
            age = int(input("Enter age (1-150): "))
            if 1 <= age <= 150:
                break
            else:
                print("❌ Age must be between 1 and 150!")
        except ValueError:
            print("❌ Please enter a valid number!")
    
    # Collect city
    city = input("Enter city: ").strip()
    if not city:
        city = "Not specified"
    
    # TODO: Collect hobbies (list)
    
    # TODO: Collect goals (list)
    
    # Create profile dictionary
    profile = {
        "name": name,
        "age": age,
        "city": city,
        "hobbies": [],  # TODO: Replace with actual hobbies
        "goals": []     # TODO: Replace with actual goals
    }
    
    return profile


def display_profile(profile):
    """Display a single profile as a formatted card."""
    print("\n" + "╔" + "═" * 48 + "╗")
    print(f"║{'👤 PROFILE CARD':^48}║")
    print("╠" + "═" * 48 + "╣")
    print(f"║ Name:    {profile['name']:<38}║")
    print(f"║ Age:     {profile['age']:<38}║")
    print(f"║ City:    {profile['city']:<38}║")
    print("╠" + "═" * 48 + "╣")
    print(f"║{'🎯 Hobbies':^48}║")
    if profile['hobbies']:
        for hobby in profile['hobbies']:
            print(f"║   • {hobby:<42}║")
    else:
        print(f"║   {'(No hobbies listed)':<43}║")
    print("╠" + "═" * 48 + "╣")
    print(f"║{'🎯 Goals':^48}║")
    if profile['goals']:
        for goal in profile['goals']:
            print(f"║   • {goal:<42}║")
    else:
        print(f"║   {'(No goals listed)':<43}║")
    print("╚" + "═" * 48 + "╝")


def main():
    """Main application loop."""
    print("\n" + "=" * 60)
    print("👤 PERSONAL PROFILE GENERATOR")
    print("=" * 60)
    
    while True:
        print("\n--- Main Menu ---")
        print("1. Create new profile")
        print("2. View all profiles")
        print("3. View single profile")
        print("4. Search profiles")
        print("5. Edit profile")
        print("6. Delete profile")
        print("7. Profile statistics")
        print("8. Quit")
        
        choice = input("\nEnter choice (1-8): ")
        
        if choice == "1":
            profile = create_profile()
            if profile:
                profiles.append(profile)
                print(f"✅ Profile created for {profile['name']}!")
                display_profile(profile)
        
        elif choice == "2":
            if not profiles:
                print("📭 No profiles yet!")
            else:
                print(f"\n📋 All Profiles ({len(profiles)} total):")
                for i, p in enumerate(profiles, 1):
                    print(f"  {i}. {p['name']} ({p['age']}, {p['city']})")
        
        # TODO: Implement options 3-7
        
        elif choice == "8":
            print("\n" + "=" * 60)
            print("👋 Thanks for using Profile Generator!")
            print("=" * 60)
            break
        
        else:
            print("❌ Invalid choice! Please enter 1-8.")


# Run the application
if __name__ == "__main__":
    main()
```

---

### **Step 3: Complete the Features**

#### **Feature A: Collect Hobbies**

Replace the `# TODO: Collect hobbies` section:

```python
# Collect hobbies
print("\nEnter hobbies (type 'done' when finished):")
hobbies = []
while True:
    hobby = input("  Hobby: ").strip()
    if hobby.lower() == 'done':
        break
    if hobby:
        hobbies.append(hobby)
    
if not hobbies:
    hobbies = ["Not specified"]
```

#### **Feature B: Collect Goals**

```python
# Collect goals
print("\nEnter goals (type 'done' when finished):")
goals = []
while True:
    goal = input("  Goal: ").strip()
    if goal.lower() == 'done':
        break
    if goal:
        goals.append(goal)
    
if not goals:
    goals = ["Not specified"]
```

#### **Feature C: View Single Profile (Option 3)**

```python
elif choice == "3":
    if not profiles:
        print("📭 No profiles yet!")
    else:
        print("\n📋 Available Profiles:")
        for i, p in enumerate(profiles, 1):
            print(f"  {i}. {p['name']}")
        
        try:
            num = int(input("Enter profile number: "))
            if 1 <= num <= len(profiles):
                display_profile(profiles[num - 1])
            else:
                print("❌ Invalid profile number!")
        except ValueError:
            print("❌ Please enter a number!")
```

#### **Feature D: Search Profiles (Option 4)**

```python
elif choice == "4":
    if not profiles:
        print("📭 No profiles yet!")
    else:
        search = input("Enter name to search: ").strip().lower()
        found = []
        for p in profiles:
            if search in p['name'].lower():
                found.append(p)
        
        if found:
            print(f"\n🔍 Found {len(found)} profile(s):")
            for p in found:
                display_profile(p)
        else:
            print(f"❌ No profiles found matching '{search}'")
```

#### **Feature E: Edit Profile (Option 5)**

```python
elif choice == "5":
    if not profiles:
        print("📭 No profiles yet!")
    else:
        print("\n📋 Available Profiles:")
        for i, p in enumerate(profiles, 1):
            print(f"  {i}. {p['name']}")
        
        try:
            num = int(input("Enter profile number to edit: "))
            if 1 <= num <= len(profiles):
                profile = profiles[num - 1]
                print(f"\nEditing: {profile['name']}")
                print("(Press Enter to keep current value)")
                
                # Edit name
                new_name = input(f"Name [{profile['name']}]: ").strip()
                if new_name:
                    profile['name'] = new_name
                
                # Edit age
                new_age = input(f"Age [{profile['age']}]: ").strip()
                if new_age:
                    try:
                        age = int(new_age)
                        if 1 <= age <= 150:
                            profile['age'] = age
                    except ValueError:
                        print("Invalid age, keeping current value")
                
                # Edit city
                new_city = input(f"City [{profile['city']}]: ").strip()
                if new_city:
                    profile['city'] = new_city
                
                print("✅ Profile updated!")
                display_profile(profile)
            else:
                print("❌ Invalid profile number!")
        except ValueError:
            print("❌ Please enter a number!")
```

#### **Feature F: Delete Profile (Option 6)**

```python
elif choice == "6":
    if not profiles:
        print("📭 No profiles yet!")
    else:
        print("\n📋 Available Profiles:")
        for i, p in enumerate(profiles, 1):
            print(f"  {i}. {p['name']}")
        
        try:
            num = int(input("Enter profile number to delete: "))
            if 1 <= num <= len(profiles):
                removed = profiles.pop(num - 1)
                print(f"🗑️ Deleted profile: {removed['name']}")
            else:
                print("❌ Invalid profile number!")
        except ValueError:
            print("❌ Please enter a number!")
```

#### **Feature G: Profile Statistics (Option 7)**

```python
elif choice == "7":
    if not profiles:
        print("📭 No profiles yet!")
    else:
        print("\n" + "=" * 50)
        print("📊 PROFILE STATISTICS")
        print("=" * 50)
        
        # Count
        print(f"Total profiles: {len(profiles)}")
        
        # Average age
        total_age = sum(p['age'] for p in profiles)
        avg_age = total_age / len(profiles)
        print(f"Average age: {avg_age:.1f}")
        
        # Youngest and oldest
        youngest = min(profiles, key=lambda p: p['age'])
        oldest = max(profiles, key=lambda p: p['age'])
        print(f"Youngest: {youngest['name']} ({youngest['age']})")
        print(f"Oldest: {oldest['name']} ({oldest['age']})")
        
        # Cities
        cities = {}
        for p in profiles:
            city = p['city']
            cities[city] = cities.get(city, 0) + 1
        
        print("\nProfiles by city:")
        for city, count in cities.items():
            print(f"  {city}: {count}")
        
        # Most common hobbies
        all_hobbies = {}
        for p in profiles:
            for hobby in p['hobbies']:
                all_hobbies[hobby] = all_hobbies.get(hobby, 0) + 1
        
        if all_hobbies:
            print("\nMost popular hobbies:")
            sorted_hobbies = sorted(all_hobbies.items(), 
                                    key=lambda x: x[1], 
                                    reverse=True)[:5]
            for hobby, count in sorted_hobbies:
                print(f"  {hobby}: {count}")
        
        print("=" * 50)
```

---

## 🎨 **Enhancement Ideas**

### **Beginner Enhancements:**

1. Add a "favorite color" field
2. Add profile creation date
3. Sort profiles alphabetically
4. Add confirmation before deleting

### **Intermediate Enhancements:**

1. Save profiles to a file (text or JSON)
2. Load profiles when program starts
3. Add profile categories (Student, Professional, etc.)
4. Export profiles to a simple report

### **Advanced Enhancements:**

1. Add profile pictures (file paths)
2. Create a "compatibility score" between profiles
3. Add tags for better searching
4. Create profile templates

---

## 📝 **Practice File**

**File Location:** `src/L1/S10/profile_generator.py`

The complete implementation follows in the next section.

---

## ✅ **Success Criteria**

Your project is complete when you can:

- [ ] Create profiles with all required fields
- [ ] Store multiple profiles in a list
- [ ] View all profiles in a summary list
- [ ] View individual profile cards
- [ ] Search profiles by name
- [ ] Edit existing profiles
- [ ] Delete profiles
- [ ] View statistics about profiles
- [ ] Handle invalid input gracefully

---

## 🎉 **Congratulations!**

You've completed **Level 1: Noob → Nerd**!

### **What You've Learned:**

- ✅ Python basics: variables, types, operators
- ✅ Control flow: conditionals, loops
- ✅ Data structures: lists, dictionaries
- ✅ Importing and using modules (`import random`)
- ✅ Error handling and debugging
- ✅ Basic testing with assert
- ✅ Building complete applications

### **What's Next?**

**Level 2: Nerd → Novice** will cover:

- Functions in depth
- File handling
- Object-oriented programming
- Exception handling
- Working with APIs
- And more!

---

**🚀 Ready for Level 2? Keep learning!**
