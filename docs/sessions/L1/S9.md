---
learning_level: "Noob → Nerd"
prerequisites: 
  - "docs/sessions/L1/S8.md"
estimated_time: "30 minutes"
session_type: "Knowledge"
learning_objectives:
  - "Create and manipulate dictionaries"
  - "Access and modify dictionary values"
  - "Use essential dictionary methods"
  - "Iterate over dictionaries"
  - "Use basic assert statements for testing"
related_topics:
  prerequisites:
    - "docs/sessions/L1/S8.md"
  builds_upon:
    - "docs/sessions/L1/S6.md"
    - "docs/sessions/L1/S7.md"
    - "docs/sessions/L1/S8.md"
  enables:
    - "docs/sessions/L1/S10.md"
  cross_refs: []
---

# Session 9: Dictionaries & Testing

**Duration:** 30 minutes  
**Type:** 📚 Knowledge  
**Level:** Noob → Nerd

---

## 🎯 **Session Overview & Agenda**

### **What You'll Learn:**

- ✅ Create dictionaries with key-value pairs
- ✅ Access and modify dictionary values
- ✅ Use essential dictionary methods
- ✅ Iterate over dictionaries
- ✅ Write basic assert statements for testing
- ✅ Build a simple gradebook application

### **Session Flow:**

| Part | Activity                        | Description                                                    |
| ---- | ------------------------------- | -------------------------------------------------------------- |
| 1    | Creating Dictionaries           | What dictionaries are and how to create them                   |
| 2    | Accessing & Modifying           | Working with dictionary values                                 |
| 3    | Dictionary Methods              | Essential methods for dictionary operations                    |
| 4    | Iterating Over Dictionaries     | Looping through keys, values, and items                        |
| 5    | Basic Testing with Assert       | Writing simple tests for your code                             |
| 6    | Hands-on Practice               | Gradebook application                                          |

---

## 📋 **Before You Begin**

### **Prerequisites:**

✅ Completed Session 8: Lists & Loops  
✅ Understand list indexing and iteration  
✅ Comfortable with `for` loops

---

## 📖 **Part 1: Creating Dictionaries**

### **What is a Dictionary?**

A **dictionary** stores data in **key-value pairs**. Think of it like a real dictionary where you look up a word (key) to find its definition (value).

```python
# A simple dictionary
person = {
    "name": "Alice",
    "age": 25,
    "city": "New York"
}

# Key: "name" → Value: "Alice"
# Key: "age" → Value: 25
# Key: "city" → Value: "New York"
```

### **Dictionary Characteristics**

- **Unordered** (in older Python) / **Ordered** (Python 3.7+)
- **Mutable** - You can change, add, or remove items
- **Keys must be unique** - No duplicate keys
- **Keys must be immutable** - Strings, numbers, tuples (not lists)
- **Values can be anything** - Any data type

### **Creating Dictionaries**

```python
# Method 1: Curly braces with key-value pairs
student = {
    "name": "Bob",
    "grade": 95,
    "subjects": ["Math", "Science"]
}

# Method 2: dict() constructor
person = dict(name="Charlie", age=30, city="Boston")

# Method 3: Empty dictionary
empty = {}
# or
empty = dict()

# Method 4: From lists of tuples
pairs = [("a", 1), ("b", 2), ("c", 3)]
my_dict = dict(pairs)
print(my_dict)  # {'a': 1, 'b': 2, 'c': 3}
```

### **Dictionary vs List**

| Feature      | List                    | Dictionary                |
| ------------ | ----------------------- | ------------------------- |
| Access by    | Index (position)        | Key (name)                |
| Syntax       | `my_list[0]`            | `my_dict["key"]`          |
| Order        | Always ordered          | Ordered (Python 3.7+)     |
| Best for     | Sequential data         | Labeled/named data        |

### ✅ **Progress Check: Creating Dictionaries**

Ensure you can:

- [ ] Create a dictionary with curly braces
- [ ] Understand key-value pair concept
- [ ] Create empty dictionaries
- [ ] Know the difference between lists and dictionaries

---

## 🔑 **Part 2: Accessing & Modifying**

### **Accessing Values**

```python
person = {
    "name": "Alice",
    "age": 25,
    "city": "New York"
}

# Method 1: Square brackets (raises KeyError if missing)
print(person["name"])  # Alice
print(person["age"])   # 25

# Method 2: get() method (returns None if missing, or default)
print(person.get("name"))        # Alice
print(person.get("country"))     # None (no error!)
print(person.get("country", "Unknown"))  # Unknown (default)
```

### **Adding & Modifying Values**

```python
person = {"name": "Alice", "age": 25}

# Add new key-value pair
person["city"] = "New York"
print(person)  # {'name': 'Alice', 'age': 25, 'city': 'New York'}

# Modify existing value
person["age"] = 26
print(person)  # {'name': 'Alice', 'age': 26, 'city': 'New York'}

# Update multiple values at once
person.update({"age": 27, "country": "USA"})
print(person)  # {'name': 'Alice', 'age': 27, 'city': 'New York', 'country': 'USA'}
```

### **Removing Items**

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# del - Remove specific key
del person["city"]
print(person)  # {'name': 'Alice', 'age': 25}

# pop() - Remove and return value
age = person.pop("age")
print(age)     # 25
print(person)  # {'name': 'Alice'}

# pop() with default (no error if missing)
country = person.pop("country", "Not found")
print(country)  # Not found

# clear() - Remove all items
person.clear()
print(person)  # {}
```

### **Checking for Keys**

```python
person = {"name": "Alice", "age": 25}

# Check if key exists
print("name" in person)     # True
print("city" in person)     # False
print("city" not in person) # True

# Safe access pattern
if "city" in person:
    print(person["city"])
else:
    print("City not found")
```

### ✅ **Progress Check: Accessing & Modifying**

Make sure you can:

- [ ] Access values using `[]` and `.get()`
- [ ] Add new key-value pairs
- [ ] Modify existing values
- [ ] Remove items with `del` and `pop()`
- [ ] Check if a key exists with `in`

---

## 🛠️ **Part 3: Dictionary Methods**

### **Getting Keys, Values, and Items**

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# keys() - Get all keys
print(person.keys())   # dict_keys(['name', 'age', 'city'])

# values() - Get all values
print(person.values()) # dict_values(['Alice', 25, 'New York'])

# items() - Get all key-value pairs as tuples
print(person.items())  # dict_items([('name', 'Alice'), ('age', 25), ('city', 'New York')])

# Convert to lists if needed
keys_list = list(person.keys())
print(keys_list)  # ['name', 'age', 'city']
```

### **Useful Dictionary Methods**

```python
# setdefault() - Get value, set if missing
person = {"name": "Alice"}
age = person.setdefault("age", 0)  # Sets "age": 0 since missing
print(age)     # 0
print(person)  # {'name': 'Alice', 'age': 0}

name = person.setdefault("name", "Unknown")  # Already exists
print(name)    # Alice (not changed)

# copy() - Create shallow copy
original = {"a": 1, "b": 2}
copy = original.copy()
copy["c"] = 3
print(original)  # {'a': 1, 'b': 2} (unchanged)
print(copy)      # {'a': 1, 'b': 2, 'c': 3}

# fromkeys() - Create dict with same value
keys = ["a", "b", "c"]
zeros = dict.fromkeys(keys, 0)
print(zeros)  # {'a': 0, 'b': 0, 'c': 0}
```

### **Quick Reference: Dictionary Methods**

| Method         | Description                              | Returns        |
| -------------- | ---------------------------------------- | -------------- |
| `get(k, d)`    | Get value for key, or default            | Value          |
| `keys()`       | Get all keys                             | dict_keys      |
| `values()`     | Get all values                           | dict_values    |
| `items()`      | Get all key-value pairs                  | dict_items     |
| `pop(k, d)`    | Remove key and return value              | Value          |
| `update(d)`    | Update with another dict                 | None           |
| `setdefault()` | Get value, set if missing                | Value          |
| `copy()`       | Create shallow copy                      | New dict       |
| `clear()`      | Remove all items                         | None           |

### ✅ **Progress Check: Dictionary Methods**

Ensure you can:

- [ ] Get keys, values, and items from a dictionary
- [ ] Use `get()` for safe access
- [ ] Update dictionaries with `update()`
- [ ] Copy dictionaries

---

## 🔄 **Part 4: Iterating Over Dictionaries**

### **Iterating Over Keys**

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# Default iteration (keys)
for key in person:
    print(key)
# Output: name, age, city

# Explicit keys()
for key in person.keys():
    print(f"{key}: {person[key]}")
```

### **Iterating Over Values**

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

for value in person.values():
    print(value)
# Output: Alice, 25, New York
```

### **Iterating Over Items (Key + Value)**

```python
person = {"name": "Alice", "age": 25, "city": "New York"}

# Most common pattern
for key, value in person.items():
    print(f"{key}: {value}")
# Output:
# name: Alice
# age: 25
# city: New York
```

### **Practical Examples**

```python
# Student grades
grades = {"Alice": 95, "Bob": 87, "Charlie": 92}

# Calculate average
total = sum(grades.values())
average = total / len(grades)
print(f"Average: {average:.1f}")  # Average: 91.3

# Find highest grade
highest_name = ""
highest_grade = 0
for name, grade in grades.items():
    if grade > highest_grade:
        highest_grade = grade
        highest_name = name
print(f"Highest: {highest_name} ({highest_grade})")

# Using max() with a key function
best = max(grades, key=grades.get)
print(f"Best student: {best}")
```

### ✅ **Progress Check: Iterating**

Make sure you can:

- [ ] Loop through dictionary keys
- [ ] Loop through dictionary values
- [ ] Loop through key-value pairs with `.items()`
- [ ] Process dictionary data in loops

---

## 🧪 **Part 5: Basic Testing with Assert**

### **What is Assert?**

`assert` is a simple way to check if something is true. If it's not, Python raises an `AssertionError`.

```python
# Basic assert syntax
assert condition, "Error message if condition is False"

# Examples
assert 1 + 1 == 2, "Math is broken!"  # Passes (no output)
assert 2 + 2 == 5, "This will fail!"  # Raises AssertionError
```

### **Using Assert for Testing**

```python
# Function to test
def add(a, b):
    return a + b

# Test the function
assert add(2, 3) == 5, "add(2, 3) should be 5"
assert add(-1, 1) == 0, "add(-1, 1) should be 0"
assert add(0, 0) == 0, "add(0, 0) should be 0"

print("All tests passed! ✅")
```

### **Testing with Dictionaries**

```python
# Function to test
def get_grade_letter(score):
    if score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    elif score >= 60:
        return "D"
    else:
        return "F"

# Test cases
assert get_grade_letter(95) == "A", "95 should be A"
assert get_grade_letter(85) == "B", "85 should be B"
assert get_grade_letter(75) == "C", "75 should be C"
assert get_grade_letter(65) == "D", "65 should be D"
assert get_grade_letter(55) == "F", "55 should be F"

# Edge cases
assert get_grade_letter(90) == "A", "90 should be A (boundary)"
assert get_grade_letter(89) == "B", "89 should be B (just below A)"

print("All grade tests passed! ✅")
```

### **Writing Good Tests**

```python
# Test normal cases
assert calculate_area(5, 10) == 50, "5 x 10 should be 50"

# Test edge cases
assert calculate_area(0, 10) == 0, "Width 0 should give 0"
assert calculate_area(1, 1) == 1, "1 x 1 should be 1"

# Test boundaries
assert get_grade(90) == "A", "90 is boundary for A"
assert get_grade(89) == "B", "89 is just below A boundary"
```

### ✅ **Progress Check: Testing**

Ensure you can:

- [ ] Write basic assert statements
- [ ] Test functions with different inputs
- [ ] Test edge cases and boundaries
- [ ] Create simple test suites

---

## 🚀 **Part 6: Hands-on Practice**

### **📁 Practice Files**

**File Location:** `src/L1/S9/`  
**Practice Files:** `01_dict_basics.py`, `02_dict_iteration.py`, `03_gradebook.py`

### **Practice File Structure**

```text
📁 src/
└── 📁 L1/
    └── 📁 S9/
        ├── 📄 01_dict_basics.py
        ├── 📄 02_dict_iteration.py
        └── 📄 03_gradebook.py
```

---

### **Script 1: 01_dict_basics.py**

```python
# Filename: src/L1/S9/01_dict_basics.py

print("=== Dictionary Basics Demo ===\n")

# Creating dictionaries
print("--- Creating Dictionaries ---")
person = {
    "name": "Alice",
    "age": 25,
    "city": "New York"
}
print(f"Person: {person}")

# Using dict() constructor
student = dict(name="Bob", grade=95, subject="Math")
print(f"Student: {student}")

# Empty dictionary
empty = {}
print(f"Empty dict: {empty}")

# Accessing values
print("\n--- Accessing Values ---")
print(f"person['name'] = {person['name']}")
print(f"person.get('age') = {person.get('age')}")
print(f"person.get('country') = {person.get('country')}")
print(f"person.get('country', 'USA') = {person.get('country', 'USA')}")

# Modifying values
print("\n--- Modifying Values ---")
person["age"] = 26
print(f"After person['age'] = 26: {person}")

person["country"] = "USA"
print(f"After adding 'country': {person}")

# Removing values
print("\n--- Removing Values ---")
removed = person.pop("city")
print(f"Removed 'city': {removed}")
print(f"After pop: {person}")

# Checking keys
print("\n--- Checking Keys ---")
print(f"'name' in person: {'name' in person}")
print(f"'city' in person: {'city' in person}")

# Dictionary with different value types
print("\n--- Mixed Value Types ---")
profile = {
    "name": "Charlie",
    "age": 30,
    "scores": [95, 87, 92],
    "active": True,
    "address": {
        "city": "Boston",
        "zip": "02101"
    }
}
print(f"Profile: {profile}")
print(f"First score: {profile['scores'][0]}")
print(f"City: {profile['address']['city']}")
```

---

### **Script 2: 02_dict_iteration.py**

```python
# Filename: src/L1/S9/02_dict_iteration.py

print("=== Dictionary Iteration Demo ===\n")

grades = {
    "Alice": 95,
    "Bob": 87,
    "Charlie": 92,
    "Diana": 88
}

# Iterating over keys
print("--- Iterating Over Keys ---")
for name in grades:
    print(f"Student: {name}")

# Iterating over values
print("\n--- Iterating Over Values ---")
for grade in grades.values():
    print(f"Grade: {grade}")

# Iterating over items
print("\n--- Iterating Over Items ---")
for name, grade in grades.items():
    print(f"{name}: {grade}")

# Practical example: Statistics
print("\n--- Statistics ---")
total = sum(grades.values())
count = len(grades)
average = total / count
print(f"Total points: {total}")
print(f"Number of students: {count}")
print(f"Average grade: {average:.1f}")

# Find highest and lowest
print("\n--- Finding Extremes ---")
highest_name = max(grades, key=grades.get)
lowest_name = min(grades, key=grades.get)
print(f"Highest: {highest_name} ({grades[highest_name]})")
print(f"Lowest: {lowest_name} ({grades[lowest_name]})")

# Filter students with A grades
print("\n--- A Students (90+) ---")
a_students = []
for name, grade in grades.items():
    if grade >= 90:
        a_students.append(name)
print(f"A students: {a_students}")

# Dictionary comprehension preview
print("\n--- Dictionary Comprehension (Preview) ---")
passed = {name: grade for name, grade in grades.items() if grade >= 70}
print(f"Passed students: {passed}")
```

---

### **Script 3: 03_gradebook.py**

```python
# Filename: src/L1/S9/03_gradebook.py

"""
📚 Simple Gradebook Application
Demonstrates dictionaries, loops, and basic testing!
"""

def get_letter_grade(score):
    """Convert numeric score to letter grade."""
    if score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    elif score >= 60:
        return "D"
    else:
        return "F"


def calculate_average(grades_dict):
    """Calculate average of all grades in dictionary."""
    if len(grades_dict) == 0:
        return 0
    return sum(grades_dict.values()) / len(grades_dict)


def display_gradebook(grades_dict):
    """Display all students and their grades."""
    if len(grades_dict) == 0:
        print("📭 No students in gradebook!")
        return
    
    print("\n" + "=" * 40)
    print("📚 GRADEBOOK")
    print("=" * 40)
    print(f"{'Name':<15} {'Score':>8} {'Grade':>8}")
    print("-" * 40)
    
    for name, score in grades_dict.items():
        letter = get_letter_grade(score)
        print(f"{name:<15} {score:>8} {letter:>8}")
    
    print("-" * 40)
    avg = calculate_average(grades_dict)
    print(f"{'Average:':<15} {avg:>8.1f} {get_letter_grade(avg):>8}")
    print("=" * 40)


# Run tests first
print("🧪 Running tests...")

# Test get_letter_grade
assert get_letter_grade(95) == "A", "95 should be A"
assert get_letter_grade(85) == "B", "85 should be B"
assert get_letter_grade(75) == "C", "75 should be C"
assert get_letter_grade(65) == "D", "65 should be D"
assert get_letter_grade(55) == "F", "55 should be F"
assert get_letter_grade(90) == "A", "90 should be A (boundary)"

# Test calculate_average
test_grades = {"A": 100, "B": 80}
assert calculate_average(test_grades) == 90, "Average of 100 and 80 should be 90"
assert calculate_average({}) == 0, "Empty dict average should be 0"

print("✅ All tests passed!\n")

# Main application
print("=" * 50)
print("📚 GRADEBOOK APPLICATION")
print("=" * 50)

gradebook = {}

while True:
    print("\n--- Menu ---")
    print("1. Add student grade")
    print("2. View gradebook")
    print("3. Update grade")
    print("4. Remove student")
    print("5. Get student info")
    print("6. Quit")
    
    choice = input("\nEnter choice (1-6): ")
    
    if choice == "1":
        name = input("Enter student name: ")
        if name in gradebook:
            print(f"⚠️ {name} already exists! Use option 3 to update.")
        else:
            try:
                score = int(input("Enter score (0-100): "))
                if 0 <= score <= 100:
                    gradebook[name] = score
                    letter = get_letter_grade(score)
                    print(f"✅ Added: {name} - {score} ({letter})")
                else:
                    print("❌ Score must be between 0 and 100!")
            except ValueError:
                print("❌ Please enter a valid number!")
    
    elif choice == "2":
        display_gradebook(gradebook)
    
    elif choice == "3":
        if len(gradebook) == 0:
            print("📭 No students in gradebook!")
        else:
            name = input("Enter student name to update: ")
            if name in gradebook:
                try:
                    score = int(input("Enter new score (0-100): "))
                    if 0 <= score <= 100:
                        old_score = gradebook[name]
                        gradebook[name] = score
                        print(f"✅ Updated: {name} - {old_score} → {score}")
                    else:
                        print("❌ Score must be between 0 and 100!")
                except ValueError:
                    print("❌ Please enter a valid number!")
            else:
                print(f"❌ Student '{name}' not found!")
    
    elif choice == "4":
        if len(gradebook) == 0:
            print("📭 No students in gradebook!")
        else:
            name = input("Enter student name to remove: ")
            if name in gradebook:
                removed_score = gradebook.pop(name)
                print(f"🗑️ Removed: {name} (score: {removed_score})")
            else:
                print(f"❌ Student '{name}' not found!")
    
    elif choice == "5":
        if len(gradebook) == 0:
            print("📭 No students in gradebook!")
        else:
            name = input("Enter student name: ")
            if name in gradebook:
                score = gradebook[name]
                letter = get_letter_grade(score)
                print(f"\n📋 Student Info:")
                print(f"   Name: {name}")
                print(f"   Score: {score}")
                print(f"   Grade: {letter}")
            else:
                print(f"❌ Student '{name}' not found!")
    
    elif choice == "6":
        print("\n" + "=" * 50)
        print("👋 Goodbye! Keep learning!")
        print("=" * 50)
        break
    
    else:
        print("❌ Invalid choice! Please enter 1-6.")
